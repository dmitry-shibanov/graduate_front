name: Test Project
on: [push]
jobs:
  Tests:
    strategy:
        fail-fast: false
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@master

    - name: Install modules
      run: npm install
      
    - name: Test react
      run: npm test